[
  {
    "template_id": "list_comprehension_filter",
    "topic": "python.list_comprehension",
    "difficulty": "beginner",
    "times_required": 5,
    "lines_allowed": 1,
    "description_template": "Create a list named {result} containing {item}s from {items} where condition({item}) is true.",
    "pattern_focus": "[item for item in items if condition]",
    "starter_context_template": "{items} already exists.",
    "expected_template": "{result} = [{item} for {item} in {items} if condition({item})]",
    "variables": {
      "result": [
        "result",
        "filtered",
        "valid_items"
      ],
      "item": [
        "item",
        "value"
      ],
      "items": [
        "items",
        "values"
      ]
    }
  },
  {
    "template_id": "dict_get_default",
    "topic": "python.dict_get",
    "difficulty": "beginner",
    "times_required": 5,
    "lines_allowed": 1,
    "description_template": "Get {key} from {data} with {default} as the fallback.",
    "pattern_focus": "dictionary.get(key, default)",
    "starter_context_template": "",
    "expected_template": "{result} = {data}.get({key}, {default})",
    "variables": {
      "result": [
        "result",
        "value"
      ],
      "data": [
        "data",
        "record"
      ],
      "key": [
        "key"
      ],
      "default": [
        "default",
        "None"
      ]
    }
  }
]
